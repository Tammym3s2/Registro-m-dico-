# Data Transformation Report for UI - Week 15

**Role:** Query Developer / MQL Constructor  
**Project:** Mundo Animal (Veterinary Management System)  

---

## 🛠️ Explanation of the Operators Used

To meet the user interface (UI) requirements and process complex reports directly from the backend, an aggregation pipeline was implemented using the following native MongoDB operators:

* **`$lookup`**: Functions like a join in relational databases. It allows cross-referencing data by combining documents from an external collection (`pets` or `sales`) with the source collection, based on a common field (`full_name`).
* **`$unwind`**: Deconstructs the arrays generated by the `$lookup` operator. By setting `preserveNullAndEmptyArrays`, it determines whether to display or discard documents that did not find an exact match.
* **`$project`**: Shapes the final structure of the JSON. It is used to rename native variables into more intuitive field names for Frontend development, discard unnecessary IDs (`_id: 0`), and perform advanced mathematical operations in real time.
* **`$sort`**: Orders the resulting documents under specific criteria (for example, sorting in descending order `-1` to display the highest revenues first).
* **`$limit`**: Restricts the number of documents sent to the UI (in this case, a Top 10), preventing the saturation of tables and graphical components in the frontend.

---

## 💻 Implemented Queries and Results

### 1. Sales Report with Associated Pet (`sales`)
This query runs from the `sales` collection to link each buyer with their registered pet data in the system.

#### MQL Query:

```
[
  {
    $lookup: {
      from: "pets",
      localField: "full_name",
      foreignField: "full_name",
      as: "datos_mascota"
    }
  },
  {
    $unwind: {
      path: "$datos_mascota",
      preserveNullAndEmptyArrays: true
    }
  },
  {
    $project: {
      _id: 1,
      cliente: "$full_name",
      correo: "$email",
      total_venta: "$total_amount",
      metodo_pago: "$payment_method",
      fecha_venta: "$sale_date",
      nombre_mascota: { $ifNull: ["$datos_mascota.pet_name", "Sin mascota"] },
      especie: { $ifNull: ["$datos_mascota.species", "N/A"] },
      color: { $ifNull: ["$datos_mascota.fur_color", "N/A"] },
      ganancia_neta: { $multiply: ["$total_amount", 0.85] }
    }
  },
  {
    $sort: {
      total_venta: -1,
      fecha_venta: -1
    }
  },
  {
    $limit: 10
  }
]

```

#### Result Summary:
The pipeline maps the `sales` collection with the clinic's patients directly. It modifies variables from the original document to structure a clean JSON response (`cliente`, `correo`, `total_venta`). It enriches the output by bringing in pet fields such as `nombre_mascota: "iris"` and `especie: "felina"`. Additionally, it applies a mathematical operation to automatically calculate the `ganancia_neta` by subtracting an estimated percentage, delivering processed information ready for the UI financial charts.

---

### 2. Pharmacy Inventory Report (`medications`)
This query processes data from available medications to calculate the total value of current stock and prioritize replenishment in the user interface.

#### MQL Query:

```
[
  {
    $project: {
      _id: 0,
      codigo: "$id_medication",
      medicamento: "$name_medication",
      laboratorio: "$laboratory",
      precio_unidad: "$price",
      disponibles: "$stock",
      especie_destino: "$specie",
      valor_total_stock: { $multiply: ["$price", "$stock"] }
    }
  },
  {
    $sort: {
      valor_total_stock: -1,
      disponibles: 1
    }
  },
  {
    $limit: 10
  }
]


```

#### Result Summary:
The pipeline transforms technical variables like `name_medication` and `stock` into clean labels (`medicamento`, `disponibles`). Using the `$multiply` function, it dynamically calculates the total investment per product (for example, showing that the dewormer HeartGard has 54 units at a price of 580, representing a total stock value of 31,320). Sorting ensures that the UI displays the medications representing the highest financial asset for the clinic first.
---

### 3. Purchase History Report by Patient (`pets`)
This query links the patient collection with the sales collection to identify client buying behavior and revenue generated per pet.

#### MQL Query:

```
[
  {
    $lookup: {
      from: "sales",
      localField: "full_name",
      foreignField: "full_name",
      as: "historial_compras"
    }
  },
  {
    $unwind: {
      path: "$historial_compras",
      preserveNullAndEmptyArrays: false
    }
  },
  {
    $project: {
      _id: 1,
      nombre_mascota: "$pet_name",
      especie: "$species",
      dueno: "$full_name",
      telefono: "$phone_number",
      total_ticket: "$historial_compras.total_amount",
      fecha_pago: "$historial_compras.sale_date",
      metodo_pago: "$historial_compras.payment_method"
    }
  },
  {
    $sort: {
      total_ticket: -1
    }
  },
  {
    $limit: 10
  }
]


```
